# langchain_ts

## 环境准备
安装bun运行时, 见[bun中文网](https://www.bunjs.cn/docs/installation)


测试是否bun正常可用
```bash
bun run index.ts
```
安装依赖
```bash
bun install
```
复制`.env.example`文件为`.env`文件，并配置自己的API KEY。


## 运行示例
运行`app/1/lcel.ts`文件
```bash
cd langchain_ts 
bun run app/1/lcel.ts
```
